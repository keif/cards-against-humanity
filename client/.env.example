# Backend server URL
VITE_SERVER_URL=http://localhost:8080

# App domain displayed in placeholders (e.g., "localhost:3000/<party_code>")
# Defaults to current host if not set
VITE_APP_DOMAIN=localhost:3000