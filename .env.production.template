# Production Environment Configuration
# Copy this to .env in production and fill in the values

# ===== Security Configuration (REQUIRED) =====

# Session secret - Use a strong random string (minimum 32 characters)
# Generate with: openssl rand -hex 32
SESSION_SECRET=REPLACE_WITH_SECURE_RANDOM_STRING

# Admin key for moderator promotion - Use a strong random string
# Generate with: openssl rand -hex 16
ADMIN_KEY=REPLACE_WITH_ADMIN_KEY

# Allowed origins for CORS - Comma-separated list of your domains
# Example: https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=https://your-domain.com

# ===== Server Configuration =====

NODE_ENV=production
PORT=8080
LOG_LEVEL=info

# ===== Redis Configuration =====
# These defaults work with docker-compose setup
REDIS_HOST=redis
REDIS_PORT=6379

# ===== Optional: Rate Limiting =====
# Uncomment and adjust if you need custom rate limiting
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
